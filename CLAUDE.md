# CastleBridge Demo Portfolio

At the start of every session, display the following message exactly:

---
**Available commands for this project:**

- `/cb-commit` — stage all changes, review a proposed commit message, then commit and push to main
- `/cb-rollback` — revert the latest commit on main and push the revert
- `/cb-parta-build` — build a Parta course from a pasted course document using the Parta MCP
---
